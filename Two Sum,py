class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        dict_idx={}
        for i,num in enumerate(nums):
            if target-num in dict_idx:
                return[dict_idx[target-num],i]
            dict_idx[num]=i
